--find-links https://download.pytorch.org/whl/cpu

# Core dependencies for CV vs JD matcher
streamlit
plotly
pandas
matplotlib
wordcloud
PyMuPDF
pdfplumber
sentence-transformers
scikit-learn
fuzzywuzzy
python-Levenshtein
spacy
openpyxl
reportlab
rapidfuzz
pandas
lightgbm
joblib

# Optional / heavy (install only if needed)
layoutparser
# detectron2  # optional, system-specific and may require CUDA/toolchain (commented: no wheel available)
# CPU-only PyTorch (much lighter than CUDA version: ~200MB instead of 3GB+)
torch==2.1.0
transformers==4.46.0

# Notes:
# - Installing 'layoutparser' with detectron2 backend can be large and
#   may require compiling/dedicated wheels. Install only when you need layout extraction.
# - To install everything (may be large):
#     python3 -m pip install -r requirements.txt
