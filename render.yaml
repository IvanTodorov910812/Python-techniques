services:
  - type: web
    name: cv-matcher-app
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: starter

    envVars:
      - key: CACHE_DIR
        value: /app/cache
      - key: PYTHONUNBUFFERED
        value: "1"

    healthCheckPath: /_stcore/health
    autoDeploy: true
